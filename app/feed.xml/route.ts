import { getAllBriefings } from "@/lib/content"; export const dynamic="force-static";
function esc(v:string){return v.replace(/[<>&'"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]||c))}
export async function GET(){const items=getAllBriefings().slice(0,30);const body=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Seven Gates Daily Brief</title><link>https://sevengatesresearch.com/briefing</link><description>Independent research on companies, markets and power.</description>${items.map(x=>`<item><title>${esc(x.title)}</title><link>https://sevengatesresearch.com/briefing/${x.date}</link><guid>https://sevengatesresearch.com/briefing/${x.date}</guid><pubDate>${new Date(`${x.date}T06:55:00Z`).toUTCString()}</pubDate><description>${esc(x.excerpt)}</description></item>`).join("")}</channel></rss>`;return new Response(body,{headers:{"Content-Type":"application/rss+xml; charset=utf-8"}})}